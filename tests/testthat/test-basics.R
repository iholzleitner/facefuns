
path_to_tem <- system.file("extdata", "tem", package="facefuns")

#test_that("errors", {
#  expect_error(basics(), ".*")
#  expect_error(basics(path = path_to_tem, rotate = "1"), "'arg' should be one of “NA”, “flipX”, “flipY”, “rotateC”, “rotateCC”", fixed= TRUE)
  # create error for when file does not exist
  # create error for when
#})
